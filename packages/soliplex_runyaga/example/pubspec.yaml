name: soliplex_runyaga_example
description: The Boiler Room — example app
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.5.0

dependencies:
  flutter:
    sdk: flutter
  soliplex_logging:
    path: ../../soliplex_logging
  soliplex_runyaga:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
